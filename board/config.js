// sportsdata-ai.com/board is a STATIC page — it animates a captured sequence of
// real market data. Run it live (Betfair + tote + the sportsdata racing engine)
// by deploying the RacingBoard backend and opening …/board/?api=wss://your-host/ws
window.MF_CONFIG = { forceReplay: true, replayUrl: "data/replay.json", apiBase: null };
